{% extends "base.html.twig" %}

{% block content %}
    {% for blog in blogs %}
        <h2><a href="{{ urlFor('blog-post', {slug: blog.slug}) }}">{{ blog.title }}</a></h2>
    {% endfor %}
    
{% endblock %}

